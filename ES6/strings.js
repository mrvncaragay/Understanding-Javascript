/*
    Strings

        -interpolate values using tick `${variable}`

 */

//using ticks

let firstname = `Marv`;
let lastname = `borlog`;

let fullname = `${firstname} ${lastname}`;

console.log(fullname);
console.log(fullname.startsWith('M'));
console.log(fullname.endsWith('g'));
console.log(fullname.includes('bo'));
console.log(fullname.repeat(5));
